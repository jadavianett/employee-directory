import React from "react";
import "./Header.css"

const Header = () => {
  return (
   
    <div class="page-header">
  <h1>Employee Directory</h1>
  <br/>
  <h5>Click on a table property to to filter or use the search bar to search for a specific employee.</h5>
</div>
  );
};

export default Header;
